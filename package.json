{
  "name": "@pi0/nuxt-cache",
  "version": "0.1.0",
  "license": "MIT",
  "main": "src/index.js",
  "files": [
    "src"
  ],
  "scripts": {
    "build": "nuxt build",
    "dev": "DEBUG=1 nuxt dev",
    "start": "nuxt start",
    "lint": "eslint --ext .vue --ext .js .",
    "test": "yarn lint",
    "release": "yarn test && standard-version && git push --follow-tags && npm publish"
  },
  "dependencies": {
    "etag": "^1.8.1",
    "hasha": "^5.2.0"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^3.1.0",
    "eslint": "^7.6.0",
    "nuxt": "^2.14.0",
    "standard-version": "^8.0.2"
  }
}
